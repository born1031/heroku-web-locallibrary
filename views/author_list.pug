extends layout

block content

	h1= title

	ul
		each author in author_list
			li
				a(href = author.url) #{author.name}
				if author.date_of_birth || author.date_of_death != empty
					span  ( #{author.life_span} )
		else
			li
				There are no authors.